[program:{{ program_name }}]
{% for key,value in program['extraConfig'].items() -%}
{{ key }}={% if key == 'command' and program['wrapper']%}{{ program['wrapper'] }} {% endif %}{{ value }}
{% endfor -%}
