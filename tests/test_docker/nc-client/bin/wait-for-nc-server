#! /bin/sh

host="$(cat /etc/gcf/gcf-model.json | jq -r .model.nc_server.host)"
port="$(cat /etc/gcf/gcf-model.json | jq -r .model.nc_server.port)"

wait-for test-tcp-port -h "${host}" -p "${port}" -- "${@}"
