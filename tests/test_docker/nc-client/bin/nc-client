#! /bin/sh

host="$(cat /tmp/gcf-model.json | jq -r .model.nc_server.host)"
port="$(cat /tmp/gcf-model.json | jq -r .model.nc_server.port)"

while :
do
    echo "Client is up" | nc -q 0 "${host}" "${port}"
    sleep 5
done
